name: HelloWorld actions 
description: My first action 
inputs: 
 who-to-greet: 
   description: 'name to greet'
   required: true 
   default: 'world'

runs: 
 using: "composite"
 steps: 
  - name: greeting
    run: echo "This is ${{ inputs.who-to-greet }} own customised action"
    shell: bash 