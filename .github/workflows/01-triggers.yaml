name: Learning Triggers
# on: workflow_dispatch
on:
  push:
    branches:
      - feature

jobs: 
  my-trigger:
    runs-on: ubuntu-latest
    steps: 
     - name: Understand Triggers
      #  run: echo "Understanding Triggers"
       run: echo "push to feature branch"
      